[pytest]
# Tests import the package as `backend.*`, so the repo's inner `backend/`
# directory must be importable no matter where pytest is invoked from.
pythonpath = .
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
